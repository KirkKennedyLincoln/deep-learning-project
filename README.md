# Deep Learning Project — Module 4

LSTM vs Transformer encoder on Azure LLM Inference Traces for time-series forecasting.

## Setup
```bash
pip install -r requirements.txt
```

## Run
Open `deep_learning.ipynb` and run all cells. Set `DOWNLOAD=True` in the first code cell to fetch the dataset automatically.
